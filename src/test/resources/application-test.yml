---
deployment-localhost-name: ${DEPLOYMENT_LOCALHOST_NAME:prodenv-jenkins-dind}

spring:
  flyway:
    enabled: false
    url: jdbc:postgresql://${deployment-localhost-name}:5432/discord_help_bot
  datasource:
    url: jdbc:postgresql://${deployment-localhost-name}:5432/discord_help_bot

app-reference:
  address: ${deployment-localhost-name}

gitea:
  address: ${deployment-localhost-name}


