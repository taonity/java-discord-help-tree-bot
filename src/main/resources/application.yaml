---
discord:
  token: ${JAVA_DISCORD_HELP_BOT_DISCORD_TOKEN}
  mainGuildId: 448934652992946176

server:
  port: 9015

app-reference:
  protocol: http
  address: host.docker.internal

spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
  datasource:
    username: d
    password: d
    url: jdbc:postgresql://host.docker.internal:5432/discord_help_bot

gitea:
  token: ${JAVA_DISCORD_HELP_BOT_GITEA_TOKEN}
  protocol: http
  address: host.docker.internal
  port: 3000
  branch-name: main
  failure-limit: 3
  git:
    username: d
    password: d
    email: d@d.d
    repoPath: /Users/gener/OneDrive/Documents/gitea/
